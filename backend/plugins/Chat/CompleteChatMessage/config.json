{
  "schema": 1,
  "description": "Extract user intent",
  "execution_settings": {
    "default": {
      "max_tokens": 1000,
      "temperature": 0.9,
      "top_p": 0.0,
      "presence_penalty": 0.0,
      "frequency_penalty": 0.0
    }
  },
  "input_variables": [
    {
      "name": "systemPrompt",
      "description": "The main prompt for the system.",
      "default": ""
    },
    {
      "name": "$intent",
      "description": "The user intent for this chat message",
      "default": ""
    }
  ]
}
