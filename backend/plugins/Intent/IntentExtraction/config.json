{
  "schema": 1,
  "description": "Extract user intent",
  "execution_settings": {
    "default": {
      "max_tokens": 1000,
      "temperature": 0.9,
      "top_p": 0.0,
      "presence_penalty": 0.0,
      "frequency_penalty": 0.0
    }
  },
  "input_variables": [
    {
      "name": "input",
      "description": "The last message in the chat.",
      "default": ""
    },
    {
      "name": "chatId",
      "description": "The chat id to pull the chat history for",
      "default": ""
    }
  ]
}
